# ── Backend ──────────────────────────────────────────────────────────────────
fastapi>=0.111
uvicorn[standard]>=0.29
opencv-python>=4.9
numpy>=1.26
websockets>=12.0

# Optional: uncomment when real RPLIDAR is connected
# rplidar-robotics>=1.3.1

# Optional: gamepad support
# pygame>=2.5

# ── App (desktop) ────────────────────────────────────────────────────────────
PyQt6>=6.7
requests>=2.31

# ── Jetson migration notes (install these on Jetson, not PC) ─────────────────
# jetson-gpio      -> replaces motor.py stub with real GPIO
# adafruit-circuitpython-pca9685
# adafruit-circuitpython-motor
#   -> replaces servo.py stub with real PCA9685 over I2C
# picamera2 is NOT needed -- IMX678 uses GStreamer / nvarguscamerasrc
